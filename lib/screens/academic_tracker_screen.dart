import 'package:flutter/material.dart';

class AcademicTrackerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Academic Tracker Screen - To be implemented',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}

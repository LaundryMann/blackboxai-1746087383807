import 'package:flutter/material.dart';

class TaskManagerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Task Manager Screen - To be implemented',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
